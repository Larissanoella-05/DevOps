## What changed

<!-- One or two sentences: what does this PR actually do? -->

## Why

<!-- The motivation: a bug, a missing feature, a cleanup. Link the issue if there is one. -->

## Type of change

- [ ] Feature (new functionality)
- [ ] Fix (bug fix)
- [ ] Chore (tooling, dependencies, config — no app behavior change)
- [ ] Docs

## How was this tested?

<!-- e.g. "Ran `npm test` locally (X/X passing)", "Ran `npm run lint`", "Started the app with `docker compose up` and checked X in the browser" -->

## Screenshots

<!-- Required for any UI change. Delete this section if not applicable. -->

## Checklist

- [ ] `npm run lint` passes locally
- [ ] `npm test` passes locally
- [ ] I reviewed my own diff before requesting review
- [ ] No secrets, API keys, or `.env` files are included in this PR
